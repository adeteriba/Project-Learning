package Leetcode;

public class TestCircle {
    public static void main(String [] args){
        // Declare an instance of Circle class called shape.
        Circle shape = new Circle();

        // Construct the instance c1 by invoking the "default" constructor
        System.out.println("Area of the Circle: " + shape.getArea() + " Radius of the Circle: " + shape.getRadius() 
                            + " Color of Circle: " + shape.getColor());
    
    }
}
